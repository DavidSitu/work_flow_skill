interface:
  display_name: "WF"
  short_description: "Explicit repo workflow and project catch-up"
  brand_color: "#0F766E"
  default_prompt: "Use $wf arche for standard architecture docs, use $wf retrofit to normalize an existing repo, and keep TODO.md/LOG.md as WF session tracking."

policy:
  allow_implicit_invocation: false
