interface:
  display_name: "WF"
  short_description: "Explicit repo workflow and project catch-up"
  brand_color: "#0F766E"
  default_prompt: "Use $wf to bootstrap, retrofit, or catch up on a project, keep TODO.md as a session-based tracker, record completed sessions in LOG.md, and work from ARCHITECTURE/current/."

policy:
  allow_implicit_invocation: false
