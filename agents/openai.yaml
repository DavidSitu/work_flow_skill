interface:
  display_name: "WF"
  short_description: "Repo workflow, architecture, and session tracking"
  brand_color: "#0F766E"
  default_prompt: "Use $wf to convert accepted product, function, and UI direction into architecture docs, milestones, subsystem boundaries, and tracked sessions."

policy:
  allow_implicit_invocation: false
